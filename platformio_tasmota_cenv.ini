; platformio_tasmota_cenv.ini - Freenove ESP32-S3 N16R8 + OV3660
; 
; ВАЖЛИВО: Для 16MB flash потрібна специфічна partition table!
; Без неї прошивка буде завантажуватись в Safeboot.

[env:tasmota32s3-webcam-ov3660]
extends = env:tasmota32s3
board = freenove_esp32s3_n16r8

; ============================================
; PARTITION TABLE — КРИТИЧНО ВАЖЛИВО!
; ============================================
; Для 16MB flash використовуємо кастомну таблицю
; Якщо не вказати — PlatformIO використовує стандартну,
; яка не підходить для Tasmota з webcam.
board_build.partitions = partitions_16mb.csv

; ============================================
; Build flags
; ============================================
build_flags = ${env:tasmota32s3.build_flags}
    -DFIRMWARE_WEBCAM
    -DUSE_WEBCAM
    -DUSE_ESP32_CAM
    -DOTA_URL='"http://ota.tasmota.com/tasmota32/release/tasmota32s3-webcam.bin"'

; ============================================
; Libraries
; ============================================
lib_extra_dirs = ${env:tasmota32s3.lib_extra_dirs}
    lib/lib_ssl
lib_ignore = ${env:tasmota32s3.lib_ignore}
